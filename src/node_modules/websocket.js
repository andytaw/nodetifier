


function WebSocket(){

	var WebSocketServer = require('ws').Server
	var wss = new WebSocketServer({ port: 23024 });

	this.Broadcast = function(message){
		wss.clients.forEach(function each(client) {
	    	client.send(data);
	  	});
	}
	
}